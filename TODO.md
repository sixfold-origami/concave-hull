# TODO

- Compat features for crates other than nalgebra (`glam`, `geo`, that one compat crate (`mint`?))
- Add linearized angle optimization
- Make a degenerate case where the split point is colinear to the edge. I think this will cause a degenerate polygon in some cases.
- Do more thorough testing with different presets
